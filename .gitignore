/camer
*.db
*.db-wal
*.db-shm
# Build-time only; the artifacts it produces under web/vendor/ are committed.
/tools/node_modules/
/tools/package-lock.json

# web/vendor/ holds generated-but-committed assets, and a global gitignore of
# "vendor/" is common enough to be worth overriding here explicitly.
!/web/vendor/
